# AGENTS.md

## Cursor Cloud specific instructions

This repository is in its initial state, containing only a `LICENSE` file (MIT). There are currently:

- No source code, applications, or services to run
- No dependency files (`package.json`, `requirements.txt`, etc.)
- No build system or test framework configured
- No lint configuration

When code is added to this repository, this section should be updated with relevant development environment instructions, service descriptions, and any non-obvious setup caveats.
